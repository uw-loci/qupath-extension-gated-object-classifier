# QuPath conventions plugin requires Java 21; the embedded Kotlin compiler
# in older Gradle releases cannot parse "25.x" version strings, so we pin the
# Gradle JVM here to override any global org.gradle.java.home that points at JDK 25.
org.gradle.java.home=/usr/lib/jvm/java-21-openjdk-amd64
